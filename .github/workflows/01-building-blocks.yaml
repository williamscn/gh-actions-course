name: 01-Building Blocks
on:
  push
jobs:
  echo-hello:
    run-on: ubuntu-latest
    steps:
      - name: Failed step
        run:
          echo "I will fail"
      - name: Say goodbye
        run:
          echo "Goodbye!"
