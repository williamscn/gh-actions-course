name: 01-Building Blocks
on:
  push
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Failed step
        run:
          echo "I will fail"
      - name: Say goodbye
        run:
          echo "Goodbye!"
